server_host = LDAP_URIS
server_port = LDAP_PORT
search_base = LDAP_BASE
query_filter = (mail=%s)
result_attribute = uid
bind = yes
version = 3
debuglevel = 0
bind_dn = LDAP_DN
bind_pw = LDAP_PASSWORD
domain = DOMAIN